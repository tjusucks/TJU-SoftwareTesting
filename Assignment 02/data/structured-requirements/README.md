# Structured Requirements (Integrated Mode)

Member2 places formal structured requirement outputs in this directory.

Expected filename pattern:

- `{feature_id}.json`
- Example: `article-lifecycle.json`

All files must follow:

- `Assignment 02/schemas/structured-requirements.schema.json`

